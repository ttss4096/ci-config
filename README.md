# ci-config

This repository stores Jenkins CI configuration for building multiple GitHub repositories.

## Structure

```text
ci-config
├── repos.yml
├── Jenkinsfile.repo
├── Jenkinsfile.weekly
└── patches